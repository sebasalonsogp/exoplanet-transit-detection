# Demo data

The first analytical slice will add one verified, locally bundled TESS light curve and
its source metadata here. The public application will not depend on a live data service.

Expected files:

- `demo/light_curve.csv` — time and flux observations.
- `demo/target.json` — target identity, mission context, attribution, and reference values.
