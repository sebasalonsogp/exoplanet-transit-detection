[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "exoplanet-transit-lab"
version = "0.1.0"
description = "An interactive workbench for exploring exoplanet transit detection."
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "astropy>=8.0,<9.0",
    "numpy>=2.0,<3.0",
    "plotly>=6.5,<8.0",
    "scipy>=1.14,<2.0",
    "streamlit>=1.57,<2.0",
]

[dependency-groups]
dev = [
    "mypy>=1.14,<2.0",
    "pytest>=8.3,<9.0",
    "ruff>=0.9,<1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/transit_lab"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
extend-exclude = ["assets"]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "app.py"]

[[tool.mypy.overrides]]
module = ["astropy", "astropy.*", "plotly", "plotly.*", "scipy", "scipy.*"]
ignore_missing_imports = true
